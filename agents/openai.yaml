interface:
  display_name: "Slide Studio"
  short_description: "Author distinctive, interactive slides and verify exports"
  default_prompt: "Use $slide-studio to create a source-grounded, deliberately designed presentation. Author and adapt the composition, then inspect the result and deliver the requested formats."
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
policy:
  allow_implicit_invocation: true
