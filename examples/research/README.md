# A complete research narrative

Build with `node scripts/build-examples.mjs`. All 8 pages share one declared synthetic dataset. The interaction uses that same fixture; update both source and the widget data when deriving a real presentation. The static comparison is not an experimental result.
